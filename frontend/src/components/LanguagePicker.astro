---
import { useTranslations, getLangFromUrl } from "src/i18n/utils";
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<div>
  <select name="languages" id="languages">
    <option selected={lang === "en" ? "true" : ""} value="EN"
      >🇬🇧&emsp;English</option
    >
    <option selected={lang === "pt" ? "true" : ""} value="PT"
      >🇵🇹&emsp;Português</option
    >
  </select>

  <style>
    img {
      /* make the flags smaller without lossing aspect ratio */
      width: 70px;
      height: auto;
    }
  </style>
</div>
